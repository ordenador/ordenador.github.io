// Variante "ia-aplicada": su contenido fue promovido al CV oficial en español
// (src/data/resume.es.js), así que hoy es un alias. La URL /cv/ia-aplicada/
// se mantiene porque ya fue compartida. Si esta postulación necesita volver a
// divergir del CV oficial, reintroducir overrides aquí.
export { resumeData } from '../resume.es';
